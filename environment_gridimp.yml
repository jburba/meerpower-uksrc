name: gridimp
channels:
  - conda-forge
  - bccp
dependencies:
  - python=3.7
  - numpy
  - matplotlib
  - scipy
  - astropy
  - astropy-healpix
  - pyfftw
  - classylss
  - pmesh
  - jsonargparse

  # Build conda environment with:
  # conda env create -f environment_gridimp.yml
  # conda activate gridimp
